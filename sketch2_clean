background (255, 255, 255);
size (720, 720);

for (int x=0+23; x< width; x += 32){
  for (int y=0+23; y < height; y += 32){
    noStroke ();
    colorMode (HSB, 360, 100, 100);
    fill (random(360), 100, 100);
      ellipse (x, y, 30, 30);
  }
}
